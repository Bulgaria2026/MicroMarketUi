export interface Product {
    id: string;
    name: string;
    description: string;
    amount: number;
    price: number;
    discount: number;
    enabled: boolean;
    createdAt: string;
    updatedAt: string;
}